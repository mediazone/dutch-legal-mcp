# Prevent permission errors during npm install
# Force npm to use local directory for cache
cache=/tmp/.npm-cache
tmp=/tmp/.npm-tmp

# Prevent global package conflicts
prefer-offline=true
package-lock=true

# Use safer installation methods
fund=false
audit=false

# Prevent automatic permission escalation
unsafe-perm=false